# SET操作付きTLルール

## 1. このルールの範囲

このルールは、手動TLへSET操作を追加し、元TLをゲーム内で扱いやすく整形するためのものです。

清書で確定するものは次のとおりです。

- 元TLの上から下への処理順
- ⭐️手動UBと⭐️なしSET発動の区別
- 矢印の処理順
- SET状態の引き継ぎと変更位置
- 書式・コメントの保持

実際の発動順、ダメージ、ボス状態、UB可能タイミングは清書では判定しません。再現動画または実プレイで検証し、検証結果だけを後から反映します。

## 2. 編成・開始SET・マスク

TL先頭に編成表を置き、直後に空行を1行入れて開始SETを書く。

```text
[(5)アオイ|(4)ネラ|(3)ツムギ|(2)ペコ|(1)シェフィ]

[-----]🅰️OFF
```

SETマスクの位置は常に左から`5・4・3・2・1`で固定する。

```text
[54321]  アオイ・ネラ・ツムギ・ペコ・シェフィ
[5----]  アオイ
[-4---]  ネラ
[--3--]  ツムギ
[---2-]  ペコ
[----1]  シェフィ
[54-21]  アオイ・ネラ・ペコ・シェフィ
```

数字を詰めたり、発動順に並べ替えたりしない。

- 原文に開始SETがある場合は、そのマスクを初期状態として保持する。
- 原文に開始SETがなく、最初の発動が⭐️なし・矢印なしなら、そのキャラを開始SETへ追加してよい。
- それ以外で開始条件を判断できない場合は`[-----]🅰️OFF`とする。

### TL前の使用キャラ対応

編成表がない場合、TL前に次の形式で正式名とTL表記が列挙されていれば、本文のTL表記を正式名へ変換する。

```text
すみれ
TL表記は波レ
タマキ
TL表記は猫
```

使用キャラ欄の登場順を1〜5としてSET番号へ対応させる。SETマスクの固定表示位置は左から5・4・3・2・1である。

```text
すみれ → 1
タマキ → 2
チエル → 3
シオリ → 4
ティア → 5
```

## 3. SET状態の基本モデル

SETは、その行でUBを直接押す指示ではない。UB可能時に自動発動できる状態へ変更する操作である。

SETマスクは「その行のキャラが発動した直後の状態」を表し、次の発動へ引き継ぐ。数字入りマスクは状態の全置換である。

- マスクがない行は、直前のSET状態を継続する。
- 同じSET状態を繰り返し書かない。
- 次の予定がなくても、明確な解除理由がなければSETを継続する。
- 終盤・TL末尾でも、解除だけを目的とした`[-----]`を追加しない。

### 原本SETの優先

原本にSETマスクがある行は、意図的な指定として固定する。

- スクリプトで上書き・削除・追加更新しない。
- そのマスクを発動後の状態として、次の行へ引き継ぐ。
- 不整合があっても自動修正せず、注釈または検証フェーズで扱う。

## 4. 判断の優先順位

1. 原本にあるSETを保持する
2. ⭐️手動UB対象の誤発を防ぐ
3. 矢印連鎖の順番を守る
4. 次の通常発動に必要なSETを準備する
5. 通常発動が続く区間でSET操作をまとめる
6. 明確な解除理由がある場合だけ解除する

「次の1発だけを残す」ことは、全行へ機械的に適用しない。

## 5. 発動種別ごとの処理

### 5.1 ⭐️手動UB

⭐️付きキャラは、その発動時点でSET外でなければならない。

- 時刻による「5秒前」基準は使用しない。
- 手動対象は、発動順とSET状態から解除位置を決める。
- 可能なら手動対象と同じキャラの直前発動へ解除を便乗し、なければ⭐️行直前の発動行へ置く。
- 同じキャラの⭐️が連続する場合、解除操作を重複しない。
- 同時刻の矢印連鎖に続く⭐️行は、⭐️を残したまま時刻を省略し、`⭐️　　→　キャラ名`として表示する。SET計算上は手動UBとして扱う。
- 手動UB後、次の同キャラ発動が⭐️なし通常発動なら、対象を直後から再SETしてよい。
- 次の同キャラ発動が⭐️または矢印なら、対象を再SETしない。

手動行のSET変更は、⭐️行へ併記せず、原則として直後の独立行へ書く。

### 5.2 ⭐️なし通常発動

⭐️なしキャラはSETによる発動である。発動直前に、そのキャラ番号がSETへ含まれていなければならない。

- 時刻付きの⭐️なし発動は、原文上の予定発動として扱う。
- 同じキャラに後続の通常発動がある場合、発動後もSETを継続してよい。
- 次の操作が通常発動なら、次のキャラの準備を現在の操作へ便乗させてよい。
- 次の操作が⭐️または矢印なら、便乗せず個別に処理する。

### 5.3 矢印

`→`は直上行と同じ時刻帯だが、処理順は上のキャラ、SET変更、矢印先キャラの順である。

矢印元の発動直後には、次の矢印先だけを追加する。後続の矢印先を先行SETしない。

```text
0:22 シェフィ　[--3-1]
　　　→　ツムギ　[-4--1]
　　　→　ネラ　　[---21]
　　　→　ペコ　　[5---1]
　　　→　アオイ　[----1]
```

- 矢印連鎖開始前に、後続の矢印先をSET外にする。
- 後続の矢印先の解除は、連鎖直前の発動行まで前倒ししてよい。直前の秒数行までSETを残さない。
- 矢印先が発動した直後、その発動済み矢印先を解除する。
- ただし、そのキャラに後続の発動予定がない場合は、発動後もSETを継続してよい。
- 次の同キャラ発動が⭐️なし通常発動なら、矢印発動後もSETを継続する。
- 矢印連鎖中は、解除可能な対象を各矢印行で分割せず、連鎖の最後の矢印行へまとめて解除してよい。
- ただし、同じキャラが後段の矢印先として再登場し、直後の操作が⭐️の場合は、解除をその⭐️後のSET変更へ便乗させてよい。
- 次の矢印先だけを追加する。
- 既にSETされている通常発動キャラは、誤発の危険がなければ維持する。
- 次の操作が通常発動なら、その対象を矢印先の操作へ便乗させてよい。
- 複数の未来矢印先をまとめてSETしない。
- 矢印後に説明が続く場合は、意味を推測して行分割しない。原文の同一行を保持する。

### 5.4 書式記号・コメント

- キャラ名は常に表示幅4文字とし、不足分は全角スペースで埋める。
- 記号付きTLでは、記号のない時間行の先頭に全角スペースを1つ付ける。
- 先頭の`△`は`🔺`へ変換する。コメント内の記号は変更しない。
- ⭐️行のキャラ名後にある通常説明には`''`を付け、既存の`//`・`''`コメント本文は変更しない。
- 手動TL中の引用符付き`"オート"`はUB条件の説明として保持し、`🅰️ON/OFF`へ変換しない。オート操作の明示的な`オートON/OFF`だけを変換する。
- バックスラッシュだけの区切り行は出力から除去する。

### 5.5 止めぽ

止めぽはメモであり、発動操作やSET変更ではない。止めぽ後の矢印も通常の矢印連鎖として処理する。

## 6. 操作をまとめるルール

SET追加は、先に安全性を満たす候補状態を作り、その後に全体を見て操作をまとめる。最初から操作数だけを減らして発動条件を欠落させてはならない。

処理の順序は次のとおりとする。

1. キャラ番号5→4→3→2→1ごとに、発動一覧を下から上へ走査して手動・通常・矢印の制約を収集する。
2. 各キャラの追加可能位置と解除可能位置を決める。
3. TLを上から下へ統合し、通常発動へ便乗できるSET変更を現在の操作へ移す。
4. 同一状態のSET表記を省略し、最後に全行を検査する。

この順序は「最大安全SET案を基にした机上の最小化」を意味する。ADDの統合は発動順どおり上から下へ行い、解除位置は下から上で確認した条件を使って最後の安全な位置へまとめる。動画・実プレイでしか判断できない実際の発動順や誤発の有無は、清書処理では確定しない。

SET変更は内部では次の操作種別に分類する。出力時は誤読防止のため、従来どおり固定位置の絶対マスクで表示する。

| 内部種別 | 意味 |
|---|---|
| `ADD` | SET対象を追加するだけ |
| `REMOVE` | SET対象を解除するだけ |
| `SET_ALL` | 5人を全員SETする |
| `CLEAR_ALL` | 5人を全員解除する |
| `REPLACE` | 原本SETなど、状態を全置換する |
| `KEEP` | SET操作なし |

矢印連鎖では、途中は`ADD`を優先してSET対象を増やし、解除が必要な対象は連鎖末尾の`REMOVE`へまとめる。`SET_ALL`や`CLEAR_ALL`は、手動対象や未来の矢印先を誤発させないことを確認できる場合だけ使う。

操作を減らすため、次の条件ではSET操作をまとめる。

### まとめてよい場合

- 次の操作が⭐️でも矢印でもない通常発動である。
- まとめる対象が通常発動キャラである。
- まとめても手動UB対象の誤発を起こさない。
- 未来の矢印先を先行SETすることにならない。
- 原本SETを変更しない。

この場合、次の発動に必要なSETを現在の発動行へ記載し、次の行のマスクを省略する。

```text
0:55　ネラ　　　[-43--]
0:54　ツムギ　　// 状態継続
⭐️0:52　アオイ
```

### まとめてはいけない場合

- 次の操作が⭐️手動UBである。
- 次の操作が矢印である。
- 未来の矢印先を先行SETすることになる。
- 解除が必要な手動対象をSETに戻すことになる。
- 動画・実プレイで未検証の一括SETを推測することになる。

## 7. SET解除の判断

解除するのは、次のいずれかに該当する場合だけとする。

- ⭐️手動UB対象を外す必要がある。
- 矢印先の先行SETによる誤発を防ぐ必要がある。
- 通常発動済みのキャラを、次の発動順のために外す必要がある。

次の予定がないことだけを理由に解除しない。解除だけを目的とする操作は追加しない。

## 8. SET変更の記載位置

- 通常⭐️なし発動の準備：対象を必要とする発動行の横
- 矢印先の追加：矢印元キャラの横
- 矢印先発動後の解除・変更：矢印先キャラの横
- ⭐️手動UBの解除：⭐️行の直前、または安全マージンを取った直前の発動行
- ⭐️行の変更：⭐️行の次行先頭へ独立記載

## 9. 🅰️ON / 🅰️OFF

- 基本状態は`🅰️OFF`。
- 元TLに明確なオート根拠がある場合だけ`🅰️ON`を使う。
- オート対象の直前だけ`🅰️ON`にする。
- オート対象の発動直後に`🅰️OFF`へ戻す。
- オート対象以外の発動待ち中は`🅰️ON`を継続しない。
- ⭐️行では`🅰️ON`にしない。
- 状態変更時だけ記載し、継続中は省略する。

## 10. コメント・検証・注釈

コメントは、明示的な修正指示がない限り内容・記号・順番を原文のまま保持する。

「最速」「微ディレイ」「欠損」などの実戦メモは意味を解釈せず、SET操作や発動条件の根拠にしない。

実際の発動順・ダメージ・ボス状態は、再現動画または実プレイで検証する。清書段階では自動判定・推測修正をしない。

⭐️付きのキャラUB行は、SETや🅰️ON/OFFの状態に関係なくレビュー対象とする。これは誤りの断定ではなく、UB発動記載の確認である。

問題がある場合は、同じTLファイル末尾へ次の形式で注釈する。

```text
問題がある行
-----
問題のある元行
記載内容についての指摘
問題の内容
---
```

## 11. 表記ルール

キャラ名欄は全角4文字幅で揃える。3文字の名前には全角スペース1個、2文字の名前には全角スペース2個を補う。キャラ名と次の項目の間は全角スペース1個とする。

```text
　　　→　アオイ　　[5----]
　　　→　ツムギ　　[--3--]
　　　→　シェフィ　[----1]
```

矢印行のインデントは、通常は`　　→　キャラ名`、記号付き行の後続は`　　　→　キャラ名`とする。同じ矢印連鎖内では起点行のインデントを維持する。同時刻の⭐️矢印は`⭐️　　→　キャラ名`とし、⭐️を残す。

時刻は`m:ss`形式に統一する。分は`0`または`1`の一桁、秒は2桁とする。範囲の後半は秒だけ記載する。

```text
1:04-03
0:57-56
```

`53`のように`0:`が省略されている場合は`0:53`へ補填する。行左側の先頭時刻だけを時刻として扱い、SETマスクやコメント中の数字・秒は変更しない。
