# TL整形ワークフロー

## 入力確認

対象ファイルを上から最後まで読み、次を抽出します。

- 編成番号とキャラ対応
- 時刻と時刻帯
- ⭐️手動UBか、⭐️なしのSET発動か
- 矢印の連鎖と処理順
- ボス行、止めぽ、注意記号
- 元TLに明記されたオート操作

元TLの時刻が降順でなくても、処理順は必ずファイルの上から下です。

## スクリプト分担

全文をAIへ毎回渡す代わりに、通常処理は次の順で実行します。

```bash
python3 scripts/format_tl.py input.txt formatted.txt
python3 scripts/add_set_operations.py formatted.txt set_tl.txt
python3 scripts/validate_tl.py set_tl.txt
```

判断理由も確認する場合は、`add_set_operations.py`へ`--report report.txt`を追加します。レポートは清書本文へ混ぜず、SET操作の監査用に使います。

AI確認が必要な箇所だけを抽出する場合は、次を実行します。

```bash
python3 scripts/review_tl.py set_tl.txt review_queue.json --source formatted.txt --json
```

レビューキューには、機械解析不能行、`--source`で指定した原本固定SET、🅰️ON、機械検証エラーだけを出力します。コメント中の「最速」「微ディレイ」「欠損」などは、判断対象として抽出しません。

問題を同じファイルへ注釈として追記する場合は、次を使います。

```bash
python3 scripts/validate_tl.py --annotate set_tl.txt
```

`format_tl.py`は表記だけを整え、`add_set_operations.py`はSET状態だけを扱います。前処理では`53`、`1:04-03`、括弧、空白、⭐️、🅰️ON/OFF、構造記号を正規化しますが、`//`以降のコメント本文は変更しません。コメントは実戦メモとして解釈せず、「最速」「微ディレイ」「欠損」などはSET操作や発動条件の根拠にしません。⭐️の解釈が不明な行、特殊なオート条件だけをAI確認の対象にします。

SET追加は、1回の上から下への走査だけで各キャラを判断するのではなく、次の4段階で処理します。

1. キャラ番号5→4→3→2→1の固定順で、各キャラの発動一覧を下から上へ走査し、⭐️手動・⭐️なし通常発動・矢印発動を制約として収集する。
2. 収集した制約から、各キャラのSET追加可能位置・手動UBの解除可能位置を決める。
3. 安全案をTL本文の上から下へ反映し、実際の発動順を確定する。
4. 上から下へADDを連続統合し、通常発動への便乗と矢印連鎖の追加をまとめる。
5. 下から上で得た解除条件を確認し、REMOVE・全解除を安全な最後の位置へまとめる。
6. 同じ状態のマスクを省略し、原本SETを固定したまま出力・検証する。手動UB後の再SETは、全体の次行ではなく同じキャラの次回種別で判断する。

SET変更は内部で`ADD`、`REMOVE`、`SET_ALL`、`CLEAR_ALL`、`REPLACE`、`KEEP`へ分類します。矢印連鎖では追加を連続させ、最後に解除をまとめます。表示は常に固定位置の絶対マスクとし、差分表記をそのままTLへ出力しません。

このキャラ別走査は次回発動を確定するためのもので、実際の発動順を時刻順へ並べ替えるものではありません。最終的なSET状態と操作位置は、必ず本文の上から下の順で確定します。

原本にSETマスクがある行は原本指定を優先します。その行のSET操作は自動更新せず、原本マスクを次の状態として引き継ぎます。不整合があっても自動修正せず、注釈または検証フェーズで扱います。

原本SETを再計算したい場合だけ、`add_set_operations.py`へ`--ignore-original-set`を指定します。このモードでも開始SETは初期状態として使用し、それ以外の原本SETを無視します。開始状態も変更する場合は`--initial`で指定します。

清書フェーズでは、SET状態と原文の書式だけを扱います。実際の発動順、ダメージ、ボス状態、UB可能タイミングの実証は清書の判定対象にしません。これらは再現動画または実プレイによる検証フェーズで確認し、必要な修正だけを検証後にTLへ反映します。

開始SETは原文に明記された数字入りマスクを優先し、原文にない場合だけ既定値`[-----]🅰️OFF`を補います。

原文に開始SETがなく、最初の発動までに⭐️や→がない場合は、最初の⭐️なし発動キャラを開始SETへ追加します。それ以外の開始時の推測は自動化せず、検証対象として扱います。

矢印連鎖は、検証前は次の一人だけを順次SETする保守的な出力にします。動画や実プレイで確認できた後、一括SETが安全と判断した箇所だけを手動で修正します。

## 状態追跡

各発動の直前と直後に、次の状態を記録します。

| 項目 | 確認内容 |
|---|---|
| 現在SET | 現在SETに入っているキャラ |
| 次の手動 | 次に来る⭐️対象と、SETから外す必要性 |
| 次のSET発動 | ⭐️なしで発動するキャラと必要番号 |
| 矢印先 | 直後に追加する次のキャラだけ |
| 解除 | 発動後に不要となるキャラ |
| オート | 対象の直前だけON、対象発動直後にOFF |

## SET追加・解除の判断

1. ⭐️手動対象をSETから外す。
2. ⭐️なし発動対象は、発動直前までにSETへ入れる。
3. 矢印連鎖は次の一人だけを直前の発動後に追加する。
4. 発動後に解除が必要な対象だけを解除する。同じキャラに後続予定がなければ、または次の同キャラ発動が通常発動なら、矢印発動後もSETを継続する。未来の矢印先は連鎖直前の安全な発動行で早めに解除し、矢印連鎖中の解除は最後の矢印行へまとめてよい。
5. 直前の行ですでに最終状態へ変更済みなら、独立SET行を追加しない。
6. 終盤で次の操作がない場合は、最後のSET状態を維持し、解除操作を追加しない。
7. ⭐️なし行のSET変更はキャラ名の横、⭐️手動行のSET変更は直後の独立行へ記載する。

## 出力順

```text
編成対応表
開始SETと🅰️OFF
元TLの各行
必要なSETマスク
止めぽ・注意事項・末尾注記
```

元のコメントは原則として残します。SET操作を付ける位置だけ、ルールに従ってキャラ名の横へ追加します。

## 保存方針

入力ファイルは上書きしません。入力名に`_set`などを付けた別ファイルへ出力し、比較できる状態を保ちます。
